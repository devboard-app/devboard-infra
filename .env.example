POSTGRES_USER=devboard_admin
POSTGRES_PASSWORD=your_postgres_password

# core_db credentials (set in devboard-core/.env)
# CORE_DB_USER=core_user
# CORE_DB_PASSWORD=your_core_password

# work_db credentials (set in devboard-work/.env)
# WORK_DB_USER=work_user
# WORK_DB_PASSWORD=your_work_password
